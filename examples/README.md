# Examples

- `generate_sample_qr.py` — generates a sample `otpauth://` QR code image
  (using the well-known RFC 6238 test secret) that you can drag into
  PyAuthenticator to try the app without needing a real account.
